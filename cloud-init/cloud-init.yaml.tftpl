#cloud-config
package_update: true
packages:
  - nginx

write_files:
  - path: /var/www/html/index.html
    owner: root:root
    permissions: "0644"
    encoding: b64
    content: ${index_html_base64}

runcmd:
  - systemctl enable nginx
  - systemctl restart nginx